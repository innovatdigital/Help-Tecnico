<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>pluBee | Planos</title>
  <!-- Favicon -->
  <link rel="shortcut icon" href="/img/admin/PLUBEE-LOGO-E-NOME.png" type="image/x-icon">
  <!-- Custom styles -->
  <link rel="stylesheet" href="/css/admin/style.css">

  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.3/font/bootstrap-icons.css">
</head>

<body>
  <div class="layer"></div>
  
  <!-- ! Body -->
  <div class="page-flex">

    <!-- ! Sidebar -->
    <%- include('../partials/sidebar-platform', {isAdmin: isAdmin}) %>

    <div class="main-wrapper">
      <!-- ! Main nav -->
      <%- include('../partials/nav-platform') %>
  
      <!-- ! Main -->
      <main class="main users chart-page" id="skip-target">
        <div class="container">
          <% if (isAdmin) { %>
            <h2 class="main-title">Planos - Administrador - Em manutenção</h2>
          <% } %>
          
          <!-- <div class="row" style="margin-top: 2rem">
            <% plans.forEach(item => { %>
              <div class="col-md-6 col-xl-3">
                <article class="stat-cards-item plans">
                  <h2 style="color: #752A7A;"><%= item.title %></h2>
                  <h1 style="color: #752A7A; font-size: 15px; margin: 12px 0 8px 0">Valor mensal: R$<%= item.value_month %></h1>
                  <h1 style="color: #752A7A; font-size: 15px;">Valor anual: R$<%= item.value_year %></h1>
                  <h4 style="margin: 22px 0px;">Descrição plano:</h4>
                  <div>
                    <% item.description.forEach(description => { %>
                      <p style="color: #686868;"><i class="bi bi-check-lg" style="color: #20bd20; font-size: 22px;"></i><%= description %></p>
                    <% }) %>
                  </div>
                  <div style="margin: 15px 0 6px 0;">
                    <h4>Ações</h4>
                    <button>Editar</button>
                    <button>Excluir</button>
                  </div>
                </article>
              </div>
            <% }) %>
          </div> -->
        </div>
      </main>

      <!-- ! Footer -->
      <%- include('../partials/footer-platform') %>
    </div>
  </div>


<!-- Chart library -->
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<!-- Icons library -->
<script src="js/admin/feather.min.js"></script>
</body>
</html>

<script>
    const btn = document.querySelector('#btn-add-link')

    btn.addEventListener("click", function() {
        const link = document.querySelector("#link");

        if (link.length == 0) {
          link.style.cssText = "background-color: red;"
        }

        fetch("groups/new/" + link.value, {
            method: "POST",
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ id_user: '63ec3ca3bdeb832d9eb38322', id_account: 1 })
        })

        location.reload();
    })
  
</script>